# Phase 0 + Phase 1 (M1 Mac / CPU): API-based LLM/VLM
# Install with: pip install -r requirements.txt

python-dotenv>=1.0.0
requests>=2.31.0
Pillow>=10.0.0

# Phase 1+ (GPU Server only): AI2-THOR requires GPU + X11
# Install with: pip install -r requirements_gpu.txt
