# Exact versions from the environment that loads + runs this checkpoint (verified: encoder 588/588,
# predictor 300/300 params loaded, forward OK). Install:  pip install -r requirements.txt
# No `xformers` needed — attention uses torch.nn.functional.scaled_dot_product_attention.
# NOTE: pick the torch 2.12.0 build matching your CUDA (e.g. --index-url .../cu128); CPU also works.

torch==2.12.0
einops==0.8.2
timm==1.0.27
numpy==2.3.5
huggingface_hub==1.19.0

# Optional — only if you decode your own videos for feature extraction (not needed to load weights):
# av>=11               # PyAV (ffmpeg) decoder
# decord>=0.6          # alternative decoder
