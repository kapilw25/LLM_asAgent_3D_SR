# iter18 (2026-06-13) — IMPROVEMENT ARM #2 (plan_outperform_FT.md Q1, lever #2).
# Goal: beat surgery_raw on future-MSE / causal-L1 by letting the FACTOR signal express —
# halve the raw-clip replay so OURS stops becoming a twin of raw.
#
# ADDITIVE: extends the OURS recipe (surgery_3stage_DI_encoder.yaml) and changes ONLY
# replay.raw_pretrain_pct. The OURS arm itself is untouched → both stay comparable in eval.
extends: surgery_3stage_DI_encoder.yaml

data:
  adapted_encoder: vjepa_2_1_surgical_3stage_DI_replay25_encoder
  output_dir: outputs/full/m09c_surgery_3stage_DI_replay25_encoder    # auto-overridden by run_train.sh

replay:
  raw_pretrain_pct: 0.25            # was 0.5 (surgery_base) — half the raw dilution (CLEAR replay; Q1 lever #2)
