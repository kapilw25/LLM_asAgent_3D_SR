# 🗂️ FactorJEPA · AAAI 2027 — Task Tracker

> 📄 30 pp · 🟢 0 errors · 🔗 0 undef refs/cites. ANON / P0 / P1 / P2 / P3 all ✅. **ACTIVE: 🖼️ FIGURES readability pass** (🎯 deal-breaker: readable at 100% zoom in the compiled PDF). ⏳ camera-ready author block · ⬜ one `neurips` item.

| 🏷️ | 🚦 | 🔎 Detail — every item preserved (one line each) |
|:--:|:--:|---|
| 🚨 **ANON** | ✅ | ✅ paper `.tex` — 0 name/email/URL hits in any built file<br>✅ HF dataset renamed `walkindia-200k` → `denseworld-115k` (card rewritten)<br>✅ code `README.md` anonymized — "for double-blind review", author `Anonymous`, 0 name/BibTeX hits<br>✅ shipping code — anon `hf_finetuned_push.py:398` = `author={{Anonymous}}` (main repo keeps the real name, correctly)<br>✅ `src/CLAUDE.md` + `src/MEMORY.md` not shipped in the payload<br>✅ `anonymousML123` — 0 in payload (runtime `repo_id`, no hardcoded org); paper shares **no** HF link → org never exposed<br>✅ `walkindia` — 0 in payload (HF keeps an un-removable old-name redirect) |
| 🔴 **P0** | ✅ | ✅ abstract ↔ body same method — §factorjepa rewritten as predictor surgery<br>✅ Latent-to-RGB removed from body (`2_factor_jepa.tex`, Cosmos 0 hits)<br>✅ 9 undefined `\ref` → `??` all remapped / de-referenced<br>✅ `8_conclusion.tex` 3 dropped claims (Cosmos / 1,000h / intervention → 276h, causal future-block, no decoder)<br>✅ Causal L1 → **causal future-block L1** (matches `pt_causal.py` + abstract + m13 label)<br>✅ `Ψ_human` removed — eq + `tab:evaluator_provenance` row + §retitled<br>✅ demo-card teaser `fig:demo_cards` (motion+turn, `NOMADIC CITY TOUR` blurred), Figure 1 atop p.2; webpage kept as playable video |
| 🟠 **P1** | ✅ | ✅ `1,000h → 276h` + clip `6–13s → 4–10s` (`2_data.tex`)<br>✅ sourcing → **714 YouTube videos @480p** (`2_data.tex` + appendix)<br>✅ DINOv2 → **Grounding DINO + SAM 3** — 4 body + 46 appendix (abstract's DINOv2 *baseline* kept, correct)<br>✅ `80/10/10` → `86,831 / 909 / 23,106` (appendix; 4,841 no-factor-target excluded)<br>✅ masking-spec `1:12 / 13:16` — moot, absent from whole build<br>✅ 2B ViT-G `16 → 26` heads, MLP `4 → 4.92` (appendix; 1B ViT-g correctly 16)<br>✅ motion-cosine → intra−inter **separation** (body + appendix)<br>⬜ `0_main_neurips.tex` 500h — not in AAAI build; reconcile only if that main ships |
| 📎 **Appendix** | ✅ | ✅ **157 build errors → 0** — 1-line `\PassOptionsToPackage{table}{xcolor}` (colortbl `\rowcolor` defined)<br>✅ Latent-to-RGB §H + outline + intro `\iffalse` (fabricated Cosmos decoder)<br>✅ Architecture + Algorithm 1 `\iffalse` (fabricated `Ŷ = C·Aᵀ`, visibility gate)<br>✅ factor-heads / synthesis-dicts / `L_factor` `L_sep` `L_sparse` `L_V` / `Ψ_V` gate — neutralized to factor-view language (verified absent from code)<br>✅ Intervention L1 → causal future-block L1 (definition + registry + metric refs)<br>✅ `liu2024groundingdino` bib entry added — all citations resolve<br>✅ latexmk clean — 30 pp, 0 errors, 0 undef refs/cites, colored tables render |
| 🟡 **P2** | ✅ | _(all written into §Ablations+Limitations)_<br>✅ factor-targets ON vs OFF **LOSE** — `surgery_raw` separates on future-frame (both scales) → gain is predictor surgery, not factor targets<br>✅ interaction-stage ON/OFF **FAIL** — no-`D_I` unchanged; separates on mcos + fut<br>✅ WiSE-FT merge **LOSE BADLY** — merged f30/f50/f70 lose to un-merged on fut + mcos (both scales, separated)<br>✅ V-JEPA 2.0 base = precondition — recipe doesn't beat frozen on the 2.0 backbone<br>✅ compute-matched control never run — declared, not run (can't fully rule out "more training")<br>✅ pretrain_head / CaSSLe / EWC never eval'd — trained, not evaluated on the suite<br>✅ `delta_5` encoder-update **win** — separates on fut / causal / maskratio (concedes mcos / rollout / exposure-bias) |
| 🖼️ **FIGURES** | ⬜ | 🎯 **DEAL-BREAKER: every text label on every plot must be readable at 100% (normal) zoom in the COMPILED PDF** — keep running visual-audit + iterating until it passes; BE CREATIVE splitting individual bar plots / forests.<br>💰 **budget**: FULL 116k trained only 3 arms (DiHeavy = best OURs · LoRA = best rival · Frozen) → rich per-rival bars exist **only at POC**.<br>✅ **Fig 6 · hero forest** → `forest_plot_frozen_ci` ➜ **`forest_plot_best_ci_paper`** (POC 2B + 1B, paper-mode): OURS separates over the BEST rival at both sizes; right-margin ×CI value column, verified readable at 100% zoom. Swapped in `2_factor_jepa.tex` L320.<br>⬜ **Fig 7 · WINNER bars (2×2)** → **`eval_scorecard`** enriched per-rival, POC. ⚠️ user-flagged: current 1×4 strip = action/taxonomy/mcos/fut → **only fut is a WIN** (action ties, taxonomy CI degenerate, **mcos LOSES** −14.5×/−9.2× @POC). **FIX**: (a) **2×2** not 1×4; (b) show only GENUINE winners; (c) **HORIZONTAL bars** (arm names on y-axis, read L→R) → kills the unreadable rotated x-axis encoder names; full 30-grid → appendix. Needs a new m13 renderer.<br>📊 evidence (×CI≥1=WIN, verified from eval_metrics.json): **fut+causal WIN BOTH scales**; maskratio+teacher_free WIN 1B-only; tcc_cycle WIN 2B-only; mcos LOSES both @POC. → ✅ **CHOSEN: Option B′** = the 2 double-scale winners (future-frame MSE · causal future-block L1) × {2B, 1B} = 2×2, every panel a genuine WIN. 🔄 **BUILT**: `plot_winner_scorecard` (dot ± 95% CI caterpillar, NOT bars-from-zero — the ~small margins would vanish; horizontal → arm names readable, FactorJEPA green vs grey rivals, sorted best-at-top, +×CI margin per panel). Swapped in tex → **Figure 7, p.7**, compiles clean; body text + caption corrected (mcos overclaim removed, full scorecard → appendix Fig 12). Mirror-layout (names on OUTER margins, 10.5pt) + shared per-row metric labels after audit round 1 flagged names <9pt.<br>🐛 **CORRECTNESS FIX (audit-surfaced)**: figures said "future-frame **MSE**" but code computes **L1** (`masked_predict_l1` = `.abs().mean()`, key `future_frame_l1_loss`); main body already said L1. Fixed the single source `configs/metric_names.json` (fut name → "future-frame L1") → all figures now correct. Also corrected the **appendix formal definition** which wrongly wrote `‖·‖₂²` (MSE) + a sentence asserting "not relabeled as L1" → now `‖·‖₁` + "matching the implementation" (✅ user-confirmed 2026-07-26: keep L1, it matches the code).<br>📐 **readability rounds** (visual-audit gated): R1 names ~7.5pt→ R2 mirror-layout+10.5pt (~8.6pt, still <9) → R3 grew figure to aspect ~1.28 (dedicated full-page fig*) + 11.5pt font → ~10pt names w/ row gaps; + suptitle now pins corpus `POC 10k (n_test=1,825)` (C9/VM14 fix). ⏳ final audit running.<br>⬜ **Fig 8 · cross-scale replication** → main body keeps **ONE panel** (1 of the 15 in `scale_replication`): **causal future-block L1** (ρ=0.978 — strongest replication of all 15 + a headline predictor win), the 2B→1B scatter rendered big so every label is readable at 100% zoom (points hug identity y=x, FactorJEPA green at the winning end). **Full 15-panel ρ grid `scale_replication` → appendix** cross-scale-robustness § (by the existing Spearman-ρ methods block, ~L5144). ↩️ data-scaling forest `scale_poc_vs_full` swap **reverted** — NOT in main.<br>⬜ **Fig 2 · scene-types** — `flyover` panel is a bad frame (black/"Comment"); replace with a real flyover image.<br>⬜ **Fig 5 · factor-construction** — hide `Tier1` / `Bangalore` markers; use a clean frame from `bangalore_drive.mp4`. |
| 🟢 **P3** | ✅ ⏳ | ✅ `\clearpage`×2 + `\newpage`×2 commented (AAAI disallows manual page breaks)<br>✅ `0_main.tex` stale `\input{10_india}` + `\input{12_new_plan}` commented (not the AAAI build)<br>✅ narrowed "we release the dataset" → "the dataset construction pipeline, checkpoints, and 15-diagnostic harness"<br>✅ clips ↔ videos — dataset-object ref → "short videos"; technical `clip` (raw / video / per-clip) kept as standard<br>⏳ author block — anonymous & fine at submission; **you**: fill for camera-ready |
