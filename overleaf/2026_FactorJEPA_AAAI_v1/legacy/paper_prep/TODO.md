# 🗂️ FactorJEPA · AAAI 2027 — Task Tracker

> 📄 33 pp · 🟢 0 errors · 🔗 0 undef refs/cites. ANON / P0 / P1 / P2 / P3 all ✅. **🔄 🖼️ FIGURES pass** — Fig 2/5/6 done + audit PASS. **Fig 7 & 8 being REBUILT to the CORRECT spec** (bar scorecard with every rival NAMED · restore Fig 8 rival names) after the caterpillar / green-grey versions were REJECTED by the user 2026-07-27 (see [[feedback_never_override_user_spec]] + audit C0). ⏳ camera-ready author block · ⬜ one `neurips` item.

| 🏷️ | 🚦 | 🔎 Detail — every item preserved (one line each) |
|:--:|:--:|---|
| 🚨 **ANON** | ✅ | ✅ paper `.tex` — 0 name/email/URL hits in any built file<br>✅ HF dataset renamed `walkindia-200k` → `denseworld-115k` (card rewritten)<br>✅ code `README.md` anonymized — "for double-blind review", author `Anonymous`, 0 name/BibTeX hits<br>✅ shipping code — anon `hf_finetuned_push.py:398` = `author={{Anonymous}}` (main repo keeps the real name, correctly)<br>✅ `src/CLAUDE.md` + `src/MEMORY.md` not shipped in the payload<br>✅ `anonymousML123` — 0 in payload (runtime `repo_id`, no hardcoded org); paper shares **no** HF link → org never exposed<br>✅ `walkindia` — 0 in payload (HF keeps an un-removable old-name redirect) |
| 🔴 **P0** | ✅ | ✅ abstract ↔ body same method — §factorjepa rewritten as predictor surgery<br>✅ Latent-to-RGB removed from body (`2_factor_jepa.tex`, Cosmos 0 hits)<br>✅ 9 undefined `\ref` → `??` all remapped / de-referenced<br>✅ `8_conclusion.tex` 3 dropped claims (Cosmos / 1,000h / intervention → 276h, causal future-block, no decoder)<br>✅ Causal L1 → **causal future-block L1** (matches `pt_causal.py` + abstract + m13 label)<br>✅ `Ψ_human` removed — eq + `tab:evaluator_provenance` row + §retitled<br>✅ demo-card teaser `fig:demo_cards` (motion+turn, `NOMADIC CITY TOUR` blurred), Figure 1 atop p.2; webpage kept as playable video |
| 🟠 **P1** | ✅ | ✅ `1,000h → 276h` + clip `6–13s → 4–10s` (`2_data.tex`)<br>✅ sourcing → **714 YouTube videos @480p** (`2_data.tex` + appendix)<br>✅ DINOv2 → **Grounding DINO + SAM 3** — 4 body + 46 appendix (abstract's DINOv2 *baseline* kept, correct)<br>✅ `80/10/10` → `86,831 / 909 / 23,106` (appendix; 4,841 no-factor-target excluded)<br>✅ masking-spec `1:12 / 13:16` — moot, absent from whole build<br>✅ 2B ViT-G `16 → 26` heads, MLP `4 → 4.92` (appendix; 1B ViT-g correctly 16)<br>✅ motion-cosine → intra−inter **separation** (body + appendix)<br>⬜ `0_main_neurips.tex` 500h — not in AAAI build; reconcile only if that main ships |
| 📎 **Appendix** | ✅ | ✅ **157 build errors → 0** — 1-line `\PassOptionsToPackage{table}{xcolor}` (colortbl `\rowcolor` defined)<br>✅ Latent-to-RGB §H + outline + intro `\iffalse` (fabricated Cosmos decoder)<br>✅ Architecture + Algorithm 1 `\iffalse` (fabricated `Ŷ = C·Aᵀ`, visibility gate)<br>✅ factor-heads / synthesis-dicts / `L_factor` `L_sep` `L_sparse` `L_V` / `Ψ_V` gate — neutralized to factor-view language (verified absent from code)<br>✅ Intervention L1 → causal future-block L1 (definition + registry + metric refs)<br>✅ `liu2024groundingdino` bib entry added — all citations resolve<br>✅ latexmk clean — 30 pp, 0 errors, 0 undef refs/cites, colored tables render |
| 🟡 **P2** | ✅ | _(all written into §Ablations+Limitations)_<br>✅ factor-targets ON vs OFF **LOSE** — `surgery_raw` separates on future-frame (both scales) → gain is predictor surgery, not factor targets<br>✅ interaction-stage ON/OFF **FAIL** — no-`D_I` unchanged; separates on mcos + fut<br>✅ WiSE-FT merge **LOSE BADLY** — merged f30/f50/f70 lose to un-merged on fut + mcos (both scales, separated)<br>✅ V-JEPA 2.0 base = precondition — recipe doesn't beat frozen on the 2.0 backbone<br>✅ compute-matched control never run — declared, not run (can't fully rule out "more training")<br>✅ pretrain_head / CaSSLe / EWC never eval'd — trained, not evaluated on the suite<br>✅ `delta_5` encoder-update **win** — separates on fut / causal / maskratio (concedes mcos / rollout / exposure-bias) |
| 🖼️ **FIGURES** | 🔄 | 🎯 **DEAL-BREAKER: every text label on every plot must be readable at 100% (normal) zoom in the COMPILED PDF** — keep running visual-audit + iterating until it passes; BE CREATIVE splitting individual bar plots / forests.<br>💰 **budget**: FULL 116k trained only 3 arms (DiHeavy = best OURs · LoRA = best rival · Frozen) → rich per-rival bars exist **only at POC**.<br>✅ **Fig 6 · hero forest** → `forest_plot_frozen_ci` ➜ **`forest_plot_best_ci_paper`** (POC 2B + 1B, paper-mode): OURS separates over the BEST rival at both sizes; right-margin ×CI value column, verified readable at 100% zoom. Swapped in `2_factor_jepa.tex` L320.<br>✅ **Fig 7 · competitive BAR scorecard (2×2)** — **GOAL**: show WHERE FactorJEPA sits vs EVERY named FT rival (full_ft · LoRA · DoRA · LP-FT · Auto-RGN · continual-SSL · frozen + all surgery variants) on the top-4 metrics — the head-to-head competitive evidence a reviewer needs.<br>**SPEC (decided 2026-07-27 · INVIOLABLE — do not drop/reformat/substitute):** 2×2 **BAR** panels, a **MIX of 2B & 1B** = the 4 (metric,scale) where OURS wins completely — future-frame L1 @2B (+6.3×) · causal future-block L1 @1B (+2.7×) · mask-ratio slope @1B (+1.9×) · free-running exposure-bias gap @1B (+1.5×); EVERY arm a NAMED bar + 95% CI, green=OURS at the winning top, HORIZONTAL bars (names read L→R), readable ≥9pt in compiled PDF; source style = `outputs/poc/probe_plot/metrics_watch/eval_scorecard_combined.png`.<br>❌ **REJECTED** (user 2026-07-27): the earlier 2-metric dot±CI caterpillar `eval_scorecard_winners` — it DROPPED 2 metrics + changed bars→dots (spec violation).<br>✅ **DONE 2026-07-27**: `eval_scorecard_winbars` — per-arm colours (`_color_for`) · short codes on bars + full-name ENCODER KEY (LoRA=low-rank adaptation, DoRA=weight-decomposed LoRA, etc., DI=interaction factor spelled out) · 45° x-ticks · **every text BOLD** · white ~78% (wasteful bands removed, rest inherent to bar charts). Visual-audit **PASS** incl. new **C11 bold-text** + **C12 space-balance** checks. Swapped into Fig 7 (`2_factor_jepa.tex`), old caterpillar `eval_scorecard_winners` → `figures/legacy/`, body text + caption updated to "four metrics", recompiled clean (**33 pp, Fig 7 p.9**). User **accepted ~7pt as-is** (dense 4-panel+key at \textwidth measures 6–8.5pt; declined landscape/split). Durable generator → `src/utils/tmp/fig7_winbars_scorecard.py` (proper m13 integration = follow-up).<br>🐛 **CORRECTNESS FIX (audit-surfaced)**: figures said "future-frame **MSE**" but code computes **L1** (`masked_predict_l1` = `.abs().mean()`, key `future_frame_l1_loss`); main body already said L1. Fixed the single source `configs/metric_names.json` (fut name → "future-frame L1") → all figures now correct. Also corrected the **appendix formal definition** which wrongly wrote `‖·‖₂²` (MSE) + a sentence asserting "not relabeled as L1" → now `‖·‖₁` + "matching the implementation" (✅ user-confirmed 2026-07-26: keep L1, it matches the code).<br>📐 **readability/sizing LESSONS** (from the now-REJECTED caterpillar — the layout knowledge carries over to the `winbars` rebuild): R1 names ~7.5pt→ R2 mirror-layout+10.5pt (~8.6pt, still <9) → R3 grew figure to aspect ~1.28 (dedicated full-page fig*) + 11.5pt font → suptitle pins corpus `POC 10k (n_test=1,825)` (C9/VM14 fix). ✅ **visual-audit PASS**: names measured **9.0pt w/ ~1.9pt inter-line gap** (deal-breaker met), C6/C9/collision/clip all PASS. (Names sit exactly on the 9pt floor — font can't go higher without clipping the longest name at the \textwidth edge; accepted.) Orphan `scale_poc_vs_full_*` (rejected Fig 8, still MSE-labeled, NOT \include'd) → moved to `figures/legacy/`.<br>⬜ **Fig 8 · cross-scale replication** → main body keeps **ONE panel** (1 of the 15 in `scale_replication`): **causal future-block L1** (ρ=0.978 — strongest replication of all 15 + a headline predictor win), the 2B→1B scatter rendered big so every label is readable at 100% zoom (points hug identity y=x, FactorJEPA green at the winning end). **Full 15-panel ρ grid `scale_replication` → appendix** cross-scale-robustness § (by the existing Spearman-ρ methods block, ~L5144). ↩️ data-scaling forest `scale_poc_vs_full` swap **reverted** — NOT in main.<br>❌ **REJECTED** (user 2026-07-27): the built single panel COLLAPSED all arms → green "FactorJEPA" vs grey "competitors", DELETING every rival's NAME. **RESTORE per-arm colours + a naming legend** (like `scale_replication.png`) so every SOTA/rival technique is identifiable; then re-verify readable. 🔄 do AFTER Fig 7 is approved.<br>✅ **Fig 2 · scene-types** — `flyover` panel WAS a promo card (taxonomy `scene_type_flyover_underpass.mp4` is 100% a "THE GOLDEN / Comment your city" title card, no footage). Replaced with a real night-aerial flyover frame from `teaser/hyderabad_flyover_underpass.mp4`, composited into the panel (rounded corners matched); original → `figures/legacy/`. Recompiled clean.<br>✅ **Fig 5 · factor-construction** (`segmented_scene.png` = compiled Figure 5, p.5) — user chose careful patch; DONE: `TIER 1` box replaced with a clean wall/tree copy from the **D_L panel** (same frame, detected offset 463 px → perfect content match, no inpaint smudge), `BANGALORE` black-filled (strokes over near-black D_A bg), `DRIVE` orange-stroke inpainted. Panel titles + `agent_pct`/`frame 3/16` intact, segmented car + privacy-blur undamaged. Zoom-verified seamless; clean in compiled Figure 5. Original → `figures/legacy/`. |
| 🟢 **P3** | ✅ ⏳ | ✅ `\clearpage`×2 + `\newpage`×2 commented (AAAI disallows manual page breaks)<br>✅ `0_main.tex` stale `\input{10_india}` + `\input{12_new_plan}` commented (not the AAAI build)<br>✅ narrowed "we release the dataset" → "the dataset construction pipeline, checkpoints, and 15-diagnostic harness"<br>✅ clips ↔ videos — dataset-object ref → "short videos"; technical `clip` (raw / video / per-clip) kept as standard<br>⏳ author block — anonymous & fine at submission; **you**: fill for camera-ready |
